---
sidebarTitle: detect_outlier_iqr
title: autogen.agentchat.contrib.captainagent.tools.data_analysis.detect_outlier_iqr.detect_outlier_iqr
---

<code class="doc-symbol doc-symbol-heading doc-symbol-function"></code>
#### detect_outlier_iqr

```python
detect_outlier_iqr(csv_file: str, column_name: str) -> 
```

    Detect outliers in a specified column of a CSV file using the IQR method.<br/>

<b>Parameters:</b>
| Name | Description |
|--|--|
| `csv_file` | The path to the CSV file.<br/><br/>**Type:** str |
| `column_name` | The name of the column to detect outliers in.<br/><br/>**Type:** str |

<br />