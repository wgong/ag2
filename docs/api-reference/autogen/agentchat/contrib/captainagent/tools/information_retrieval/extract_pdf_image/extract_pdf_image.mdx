---
sidebarTitle: extract_pdf_image
title: autogen.agentchat.contrib.captainagent.tools.information_retrieval.extract_pdf_image.extract_pdf_image
---

<code class="doc-symbol doc-symbol-heading doc-symbol-function"></code>
#### extract_pdf_image

```python
extract_pdf_image(
    pdf_path: str,
    output_dir: str,
    page_number=None
) -> 
```

    Extracts images from a PDF file and saves them to the specified output directory.<br/>

<b>Parameters:</b>
| Name | Description |
|--|--|
| `pdf_path` | The path to the PDF file.<br/><br/>**Type:** str |
| `output_dir` | The directory to save the extracted images.<br/><br/>**Type:** str |
| `page_number=None` |  |

<br />