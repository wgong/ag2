---
sidebarTitle: has_self_loops
title: autogen.graph_utils.has_self_loops
---

<code class="doc-symbol doc-symbol-heading doc-symbol-function"></code>
#### has_self_loops

```python
has_self_loops(allowed_speaker_transitions: dict[str, list[Agent]]) -> bool
```

    Check if there are self loops in the allowed_speaker_transitions.<br/>

<b>Parameters:</b>
| Name | Description |
|--|--|
| `allowed_speaker_transitions` | A dictionary of keys and list as values.<br/><br/>The keys are the names of the agents, and the values are the names of the agents that the key agent can transition<br/><br/>**Type:** dict[str, list[[Agent](/docs/api-reference/autogen/Agent)]] |

<b>Returns:</b>
| Type | Description |
|--|--|
| bool | True if there are self loops in the allowed_speaker_transitions_Dict. |

<br />